services:
  - type: web
    name: gold-price-playwright
    env: python
    plan: free
    buildCommand: "pip install -r requirements.txt && python playwright_install.py"
    startCommand: "python app.py"
